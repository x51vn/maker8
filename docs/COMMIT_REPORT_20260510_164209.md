# Commit Report

## Summary

- **Project:** /home/beou/IdeaProjects/maker8
- **Branch:** x51-commit/20260510-fix-reliability-and-scene-clip-select (new)
- **Source branch:** feature/XST-1068-uploader-identity-cleanup
- **Mode:** single project
- **Timestamp:** 2026-05-10 16:42:09
- **Branch decision:** New branch — changes are large (620 insertions/122 deletions across 13 modified files + 50 untracked), span two distinct OpenSpec changes, and are unrelated to the current ticket branch.
- **Commit title:** feat: pipeline reliability + scene clip selection (2 OpenSpec changes)
- **Push target:** origin/x51-commit/20260510-fix-reliability-and-scene-clip-select

## Uncommitted Audit

Two completed OpenSpec changes, fully implemented, 435/435 tests passing:

1. **`fix-pipeline-reliability-and-schema-sync`** (32/32 tasks):
   - Graceful SIGTERM shutdown with in-flight job drain
   - Pipeline stage retry correctness (VALIDATE/RESOLVE_ASSETS retryable; RENDER non-retryable)
   - Dead Letter Queue emission on terminal failures
   - Contract schema sync (`render_contracts/__init__.py` re-exports, schema regeneration)

2. **`wire-scene-detect-to-composer`** (16/16 tasks):
   - `Layer.scene_clip_select: str | None` field added to wire model
   - `RenderInput.scene_candidates` wired from `ctx.scene_candidates`
   - `_resolve_scene_trim()` in composer with 6 strategies: auto/first/last/longest/shortest/index:N
   - `build_layer_clip` / `_build_video` accept `effective_trim` override
   - Schema regenerated

Also includes: OpenSpec tooling setup (`.opencode/`, `.claude/`, `.codex/`, `.serena/`, `openspec/`, `AGENTS.md`, `scripts/generate_schemas.py`) and agent AI configuration dirs.

## Files Planned For Commit

### Modified — fix-pipeline-reliability-and-schema-sync
- `src/maker8/app.py`: graceful shutdown (SIGTERM handler, drain loop)
- `src/maker8/config.py`: shutdown timeout config
- `src/maker8/kafka/consumer.py`: shutdown-aware consume loop
- `src/maker8/kafka/producer.py`: producer flush on shutdown
- `src/maker8/pipeline/orchestrator.py`: retry correctness + DLQ emission
- `src/render_contracts/__init__.py`: re-export additions for schema sync
- `docs/schemas/render_request.schema.json`: regenerated (includes scene_clip_select)
- `docs/schemas/render_result.schema.json`: regenerated
- `tests/test_contracts.py`: contract label coverage additions

### Modified — wire-scene-detect-to-composer
- `src/maker8/pipeline/render.py`: wires `ctx.scene_candidates` → `RenderInput`
- `src/maker8/rendering/composer.py`: `RenderInput.scene_candidates`, `_resolve_scene_trim`
- `src/maker8/rendering/layers.py`: `effective_trim` parameter
- `src/render_contracts/render_spec.py`: `Layer.scene_clip_select`

### New — tests
- `tests/test_graceful_shutdown.py`: shutdown drain tests
- `tests/test_pipeline_retry.py`: retry correctness tests
- `tests/test_scene_clip_selection.py`: 12 unit tests for `_resolve_scene_trim`

### New — tooling / specs
- `openspec/` (config + 2 complete changes with all artifacts)
- `scripts/generate_schemas.py`
- `AGENTS.md`
- `.opencode/`, `.claude/`, `.codex/`, `.serena/` (agent tooling config)

## Detailed Changes

- `app.py`: Adds SIGTERM signal handler; registers shutdown event; loops draining in-flight consumer messages before exit
- `config.py`: `shutdown_drain_timeout_sec` added
- `consumer.py`: Checks shutdown flag before each poll; exits cleanly when flagged
- `producer.py`: Flushes producer buffer on shutdown signal
- `orchestrator.py`: Stage retry table corrected (RENDER → non-retryable); DLQ topic emission on terminal failure path
- `render_spec.py`: `scene_clip_select: str | None = None` on `Layer`
- `composer.py`: `SceneBoundary`, `Layer`, `Trim` imported; `RenderInput.scene_candidates` field; `_resolve_scene_trim()` function (~100 lines) with all strategy branches and warning codes; `_build_scene` calls `_resolve_scene_trim` per video layer
- `layers.py`: `Trim` imported; `build_layer_clip` + `_build_video` gain `effective_trim: Trim | None = None`; trim logic uses `effective_trim` if set, else `layer.trim`
- `render.py`: `scene_candidates=ctx.scene_candidates` added to `RenderInput(...)` constructor

## Change Content Summary

- **Behaviour:** Workers now drain in-flight jobs on SIGTERM before exiting; pipeline stages have correct retryability; terminal failures emit to DLQ; video layers can auto-select clip segments from detected scene boundaries
- **Config:** `shutdown_drain_timeout_sec` settable via env/config
- **Schema:** `render_request.schema.json` gains `scene_clip_select` on Layer; `render_result.schema.json` updated
- **Tests:** 3 new test files, existing 422 tests passing + 13 new = 435 total

## Affected Flows

- **Runtime:** Kafka consumer shutdown; pipeline retry/DLQ on VALIDATE/RESOLVE_ASSETS/RENDER failure
- **Render flow:** `SceneDetectStage` → `ctx.scene_candidates` → `RenderInput` → `_resolve_scene_trim` → `build_layer_clip(effective_trim=…)`
- **API/wire format:** `Layer.scene_clip_select` is a new optional field in `RenderRequest`; fully backward compatible (defaults `null`)
- **Dev workflow:** `scripts/generate_schemas.py` for schema regeneration; `openspec` CLI for change management

## Impact And Risks

- **Impact:** Medium-high — touches consumer loop, orchestrator retry logic, composer layer building
- **Risk:** Low — all changes default-safe; `scene_clip_select=null` is backward compatible; shutdown drain is bounded by timeout
- **Mitigation:** 435/435 tests pass; no regressions detected

## Untracked Files

- `.opencode/`, `.claude/`, `.codex/`, `.serena/`: staged — agent tooling, no secrets
- `AGENTS.md`: staged — project agent instructions
- `openspec/`: staged — change specs and artifacts
- `scripts/generate_schemas.py`: staged — schema generation utility
- `tests/test_graceful_shutdown.py`, `tests/test_pipeline_retry.py`, `tests/test_scene_clip_selection.py`: staged — new test files
- `docs/COMMIT_REPORT_20260510_164209.md`: staged — this report

## Sensitive / Risk Review

- **Secrets:** No `.env`, credentials, tokens, or private keys in changed files
- **Generated files:** `docs/schemas/*.json` — generated, intentionally committed as contract artifacts
- **Large files:** None
- **Deleted files:** None

## Verification

```bash
python -m pytest tests/ -q --tb=no
```

Result:

```
435 passed in 4.50s
```

## Commit Comments

Two completed OpenSpec changes implemented and verified:

1. fix-pipeline-reliability-and-schema-sync (32/32 tasks):
   graceful SIGTERM shutdown, pipeline retry correctness,
   DLQ emission, render_contracts re-export sync, schema regen

2. wire-scene-detect-to-composer (16/16 tasks):
   Layer.scene_clip_select field, RenderInput.scene_candidates wiring,
   _resolve_scene_trim with 6 strategies, effective_trim override in layers

Also adds OpenSpec tooling setup (openspec/, scripts/, AGENTS.md, agent configs).

435/435 tests pass. No regressions.

## Notes

- Both OpenSpec changes are ready to archive after this commit.
- `update-ui-editor8-rest-first-channel-ingest-auth` change (9/9) was completed before the last commit — not included here.
